# Heligoland
Micro language, interpreter and IDE.

## Decisions
1. Grammar does not support changing values of the variables, so they are treated as immutable after an assignment
2. `out` operator can output any expression
